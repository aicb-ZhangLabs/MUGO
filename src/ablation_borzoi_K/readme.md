# overview 

use to generate some final results for paper, like table for ablation and benchmark with baseline etc. 
use each of 4 generate data file to get gain/gtex-enrichment/gwas-enrichment/polyp-enrichment data and plot(maybe), beacues folder is organized different before and we only run blood tissue for ablation for K before, so we create a new folder to generate and save those data in a new folder like results/ablation_borzoi_K. then use generate table scirpt to generate final ablation table. 